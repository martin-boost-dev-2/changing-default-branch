[tool.poetry]
name = "changing-default-branch"
version = "0.1.0"
description = ""
authors = []
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
fastapi = "<1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
